"use strict"; // treat all JS code as newer version(You don't need to write this as it's already been followed by javascript)

// alert( 3 + 3 ) // we are using nodejs, not browser so it will throw error

console.log(3 + 3); // code readability should be high(6)

console.log("rakesh");

let name = "rakesh"; // string datatype
let age = 26; // number datatype
let isLoggedIn = false; // Boolean datatype
let state; // way to declare the undefined datatype

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

// object

console.log(typeof "rakesh"); // This returns the "string" type
console.log(typeof age); // This returns the "number"
console.log(typeof null); // This returns the "object" type
console.log(typeof state); // This returns the "undefined" type
